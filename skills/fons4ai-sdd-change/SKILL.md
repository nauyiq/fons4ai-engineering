---
name: fons4ai-sdd-change
description: "Fons4AI 已有内容变更工作流。用于已有功能调整，以及低风险的小配置、文档、样式、无行为重构和局部实现修改：满足内置 S0 条件时直接修改并做最小充分验证；涉及业务规则、AC、公共契约、数据、安全或更大影响时，按 S1/S2 澄清变更、生成 CR 和增量任务后停止等待实现确认。全新业务能力优先使用 fons4ai-sdd-feature-workflow，BUG 优先使用 fons4ai-bugfix-workflow。"
---

# Fons4ai-sdd-change

## Contract

### Inputs

- Required: 足以定位受影响内容的已有功能变更请求；S1/S2 模式还需要目标既有功能目录或足以唯一定位已有 SDD 功能的事实。
- Optional: 需求说明书、技术设计说明书、任务规划、历史 CR、实施报告、相关源码、测试、SQL、知识卡片和项目规则。
- Forbidden assumptions: 不得把 BUG 修复、需求变更、数据语义变更和契约变更混同；不得推倒重写既有功能；不得编造影响范围、DDL 证据或用户确认。

### Preconditions

- Entry gates: 满足触发门禁，并先执行 S0/S1/S2 风险预判；S1/S2 模式必须唯一定位目标功能目录，多候选时先询问用户选择。
- Required source artifacts: S0 只读取受影响文件、直接相关测试和必要规则；S1/S2 必须读取目标功能目录中的需求、设计、任务、changes 和 reports。
- Required repository facts: S0 必须确认预期范围、硬性排除项和快速验证方式；S1/S2 必须定向搜索模块、接口、数据结构、测试和规则，结构变更必须有 DDL/schema 证据。

### Outputs

- May create or update: S0 模式可修改明确范围内的源码、测试、配置或文档；S1/S2 模式可更新 `spec/features/<yyyymmdd>/changes/CR-xxx.md`、受影响的 SDD 产物和任务规划中的增量任务。
- Must not create or update: S0 不生成 CR、任务规划或实施报告；所有模式都不得修改知识库正文、创建知识同步任务或直接执行生产 DDL。
- Output location: S0 使用受影响文件原位置；S1/S2 使用目标 feature 目录及其 `changes/` 子目录。

### Exit Criteria

- Success: S0 模式已完成最小修改和最小充分验证并在对话中交付；S1/S2 模式已生成或更新 CR、同步受影响 SDD 产物、追加增量任务并保持 `pending`，且可用校验通过。
- Blocked: S0 缺少授权、预期范围、快速验证或出现风险升级；S1/S2 目标功能不唯一、变更意图不清、源产物冲突、DDL/schema 证据缺失、影响范围超过 70% 或需要用户裁决。
- Failure report: 输出阻塞原因、最高影响澄清问题、受影响产物、建议返回的技能或下一步。

### Handoff

- Next skill: S0 默认无下游；S1/S2 仅当用户最新消息明确确认执行后，交给 `fons4ai-sdd-implement`。
- Required handoff fields: S0 只需变更文件、验证结果和未验证项；S1/S2 需 CR 路径、变更类型、SDD 等级、已更新产物、新增任务 ID、数据结构影响、验证结果和长期知识影响。
- Stop condition: S0 在用户已明确要求修改时直接执行，不重复确认；S1/S2 生成 CR 和增量任务后停止，不自动进入实现。
- Structured handoff: 阶段交接时同时输出结构化 handoff YAML（参考 `templates/handoff-yaml-template.yaml`），供跨平台多 Agent 协作使用。

## Evidence Required

### Evidence Levels

- L1 Context Evidence: 已读取的既有 SDD 产物、历史 CR、报告、规则、代码或文档，可支撑普通影响分析。
- L2 Decision Evidence: 用户确认、正式 SDD 产物、源码事实、接口定义、测试结果或历史实施报告，可支撑变更分类、影响范围和增量任务。
- L3 Gate Evidence: 真实 DDL/schema、`.specify/sql` 快照、只读数据库查询、仓库迁移文件、执行确认或用户明确确认，可支撑数据结构变更结论。

### Hard Evidence Gates

- 数据结构、DDL、字段、索引、约束和迁移影响必须有 L3 证据；证据不足时不得生成可执行增量任务。
- 公共 API、RPC、消息、权限、安全和事务边界变化至少需要 L2 证据。
- 推断只能作为影响分析，不得单独作为 AC、DDL、公共契约或完成状态依据。

### Evidence Output

- CR 必须列出本次使用的关键证据；涉及结构变更时必须同步技术设计的 `结构变更详设` 和证据状态。
- 证据不足时必须输出待确认项或阻塞原因，不得把推断写成已确认变更事实。

## 触发门禁

使用本技能前，必须确认至少满足以下任一条件：

1. 用户明确指定该技能，例如 `$fons4ai-sdd-change`。
2. 用户明确要求迭代、调整、变更已有功能，或修改小配置、文档、样式、局部实现、无行为重构。
3. 当前仓库作用域内存在启用路由的 `AGENTS.md`，且用户当前意图明确匹配已有内容变更。

如果用户只是泛化要求“用 SDD 开发新功能”或“走 Fons4AI 工作流”，优先使用 `fons4ai-sdd-feature-workflow`，不要直接触发本技能。

如果以上条件都不满足，不得自动应用本技能。应继续使用普通 AI agent 行为，或询问用户是否希望启用 Fons4AI 工作流。

## 角色说明

你负责已有内容的风险分级和变更执行：S0 时作为轻量执行者直接完成最小修改；S1/S2 时作为技术负责人，把增量变更转化为可追踪、可验证、可执行的 CR 和增量任务。

你的目标不是用同一套重流程处理所有变化，而是：

- 先判断是否满足 S0；满足时直接修改，不生成 CR。
- 不满足 S0 时判断变更类型，以及是否会改变已有需求、AC、技术设计、数据结构或公共行为。
- 基于已有需求说明书、技术设计说明书、任务规划、CR、报告和代码事实做影响分析。
- 只更新受影响的 SDD 产物，保留未受影响内容。
- 将真正可执行的增量任务追加到 `<功能中文名>-任务规划.md`，并在 CR 中引用新增任务 ID。
- 生成 CR 后停止，等待用户明确确认实现。

## 职责边界

本技能负责 S0 直接变更，以及 S1/S2 变更分析、CR 生成、受影响 SDD 文档更新和增量任务规划。

本技能不得：

- 在 S1/S2 模式编写、修改或输出业务代码。
- 调用 `fons4ai-sdd-implement`。
- 因变更而推倒重写整个功能，除非确认超过 70% 功能受影响并建议新建 feature。
- 生成知识同步任务、知识汇总交接任务或知识库正文。
- 直接执行数据库 DDL。
- 删除既有 AC、任务或文档，除非用户明确确认。

## S0 Fast Change

### S0 硬性排除项

满足任一项即不是 S0：

1. 新增或改变业务规则、业务口径、AC 或状态流转。
2. 修改公共 API、RPC、消息、事件或外部契约。
3. 修改数据库结构、数据语义或 Redis、MQ、ES 等数据服务结构。
4. 涉及权限、安全、认证、隐私、风控、资金、支付、库存、对账或监管数据。
5. 跨核心模块或跨服务，涉及迁移、兼容方案或高成本回滚。
6. 预计超过 5 个手工修改文件，预期结果不明确，或无法快速、聚焦验证。

### S0 全部准入条件

- 修改目标单一，属于同一模块或紧密相关文件。
- 不产生任何硬性排除项。
- 变更容易撤销，不需要迁移。
- 用户已经明确要求修复、修改、实现、执行或继续；仅要求分析、查看、评估或方案时不得写文件。
- 有与修改面匹配的最小充分验证方式。

项目规则可收紧 S0，但不得默认放宽数据、契约、安全和关键业务硬门禁。没有项目规则时直接使用以上内置默认值。

### S0 执行流程

1. 读取受影响文件、直接相关测试和必要规则，不默认读取完整知识库或历史 SDD。
2. 在对话内部完成 S0 判断，不生成前置自查、CR 或任务规划。
3. 用户已授权时直接执行最小修改，不附带无关重构，不启动多 Agent 编排。
4. 按修改面选择验证：文档做格式/链接检查，配置做解析或专项校验，局部源码做聚焦测试或模块检查，无行为重构运行受影响测试。
5. 不默认执行全仓库编译或全量测试；只有命令真实成功才能声明测试或编译通过。
6. 最终只汇报修改内容、实际文件、验证方式与结果、未验证项和升级风险，不生成仓库报告。
7. 实现中发现范围扩大或硬性排除项时立即停止，保留事实并升级到 S1/S2；不得自动扩大修改范围。

## 必要上下文

以下上下文要求适用于 S1/S2；S0 使用上节规定的最小上下文：

1. 读取 `../fons4ai-sdd-requirements/references/sdd-artifact-contract.md`，包括上下文加载、实现确认和 DDL 边界规则。
2. 识别目标 `spec/features/<yyyymmdd>/` 目录。若多个功能目录匹配，先让用户选择。
3. 读取目标功能目录中的：
   - `<功能中文名>-需求说明书.md`
   - `<功能中文名>-技术设计说明书.md`
   - `<功能中文名>-任务规划.md`
   - `changes/CR-*.md`
   - `reports/*.md`
4. 按变更意图、AC ID、模块、API、领域对象、表/模型名和 SQL 路径搜索相关源码、测试、SQL、规则和知识卡片。
5. 按需读取 `.specify/rules/sdd团队协作规范.md`，用于判断标准扩展场景、S2 条件、评审门禁和完成定义。
6. 只读取会影响本次变更的上下文。只有跨领域、S2 或全局约束决策时，才读取项目级知识库总览。
7. 使用 `assets/templates/change-template.md`。

## S1/S2 变更澄清门禁

在分配正式 CR ID、更新 SDD 产物或追加任务前，必须先判断是否存在阻塞歧义。

阻塞歧义是指任何会改变以下判断的缺失或冲突信息：

- 本次请求是 BUG 修复、需求变更、数据语义变更、技术重构、纯文档修正还是新功能。
- 变更后的业务含义、术语、对象命名、归属、身份、状态、生命周期或兼容性承诺。
- 新增、删除或变更的 AC，以及期望的可观测行为。
- 公共 API、UI、消息、任务、权限/安全、集成契约、错误处理或回滚预期。
- 数据模型、字段命名、表分组、DDL 证据来源、数据库/服务归属、迁移或 fallback 策略。
- 受影响功能目录、影响模块/文件、S1/S2 等级、风险门禁、测试或长期知识影响。

规则：

1. 如果存在阻塞歧义，只问一个最高影响问题并停止。
2. 当可选项明确时，提供 2-5 个选项，并给出推荐选项和理由。
3. `按推荐`、`方案 A` 或精确短答可视为澄清完成。
4. `使用 SDD`、`继续`、`先生成`、`看一下`、产物存在或历史上下文，不等于澄清完成。
5. 用户明确要求草案时，只能生成 `文档状态：草案-待确认` 的草案 CR，不得生成可执行增量任务。
6. 正式 CR 不展示澄清门禁表、澄清状态、问题日志或内部推理过程。

## S1/S2 变更规划流程

1. 确认目标功能和变更意图。
   - 如果变更属于已有能力的标准化扩展场景，例如新增支付渠道、物流渠道、短信渠道、三方服务渠道、报表类型、审批流、策略类型、Provider、Adapter、Strategy、Handler 或 Connector，必须优先识别已有专业工作流、领域知识卡片或团队规则。
   - 标准扩展场景不等于全新功能；如果不改变公共抽象、数据模型或公共契约，优先按 S1 Change 处理。
   - 如果需要改变公共抽象、公共契约、数据结构、资金/安全链路或核心状态流转，升级为 S2 Change。
2. 执行现状一致性检查：
   - `<功能中文名>-任务规划.md` 是否存在未完成任务。
   - 历史 CR 是否存在未完成增量任务。
   - 任务状态是否与源码/测试/报告事实明显冲突。
   - 技术设计中的接口、数据结构、模块路径是否与当前代码明显不一致。
   - 如果存在不一致，先说明风险，并让用户选择先修正事实、合并处理或继续承担风险。
3. 判断变更类型：
   - `微调`：不新增 AC、不改数据结构，主要调整已有行为或文案。
   - `扩展`：新增 AC 或新增能力，但不破坏既有结构。
   - `重构`：调整核心逻辑或结构，影响多个已有任务或模块。
   - `数据结构变更`：新增、删除、重命名或修改表、字段、索引、约束、关系。
   - `契约变更`：公共 API、RPC、消息、UI 对外行为或错误码变化。
   - `纯文档修正`：只修正文档事实，不改变实现。
4. 判断 SDD 等级：
   - S1：局部行为调整、小扩展、单模块或少量模块协作。
   - S2：数据库结构、公共 API/契约、权限安全、兼容迁移、事务边界、跨核心模块或高回滚成本。
5. 判断是否应新建 feature：
   - 超过 70% AC 被替换。
   - 核心业务流程重写。
   - 数据模型主结构重构。
   - 公共 API 大范围变化。
   - 若满足以上任一情况，建议新建功能目录或拆成多个 CR，不直接推倒重写既有产物。
6. 生成影响分析：
   - 需求与 AC 变化。
   - 技术设计影响。
   - 数据结构与 DDL 影响。
   - 代码影响。
   - 测试和回归影响。
   - 回滚、兼容、安全、事务和发布风险。
   - 长期知识影响。
   - 若属于标准扩展场景，补充专业工作流复用分析：可复用工作流/规则/卡片、差异点、禁止修改的公共抽象、必须落入设计和任务的约束。
7. 确定编号：
   - 扫描 `changes/CR-*.md`，生成下一个连续 CR ID。
   - 扫描 `<功能中文名>-任务规划.md` 现有 `Txxx`，新增任务从最大编号之后继续。
   - 不复用已完成、已删除或已废弃任务编号。
8. 更新受影响文档：
   - 小范围变更：只更新受影响章节并追加变更记录。
   - AC 改动：同步更新需求说明书和任务规划。
   - 技术路径改动：同步更新技术设计说明书。
   - DDL 改动：同步更新技术设计说明书和任务规划。
   - 不允许为了一个 CR 重写整个需求或设计文档，除非用户确认。
   - 任何正式 SDD 文档的正文修改必须同步更新文档头部 `更新日期` 为本次修改日期，并在 `版本修订记录` 中追加本次变更记录。
9. UI 变更设计 Gate（仅当变更涉及页面、控制台、前端、模板引擎等交互型交付物时触发）。
   - 如果变更范围涉及页面、控制台、前端、模板引擎页面（如 Freemarker、Vue、React）、管理后台或可视化界面，必须检查是否已存在 UI 设计确认。
   - 若已有 UI 设计确认（如技术设计说明书中包含页面信息架构、布局方案、交互流），直接引用。
   - 若缺少 UI 设计确认，必须在 CR 中标注"UI 设计待确认"，并在增量任务中追加 UI 设计确认任务。
   - 不得在缺少 UI 设计确认的情况下，将页面类增量任务标记为可直接执行。
10. 创建 `changes/CR-xxx.md`。
11. 将可执行增量任务追加到 `<功能中文名>-任务规划.md`。
    - CR 只记录任务摘要和新增任务 ID。
    - `fons4ai-sdd-implement` 只从 `<功能中文名>-任务规划.md` 执行任务。
    - 每个增量任务必须包含 `AC:`、`Files:`、`Verification:`、`Quality:`、`Done:`。
    - 如果 CR 引用了专业工作流，必须把关键约束拆入新增任务的 `Quality:`、`Verification:` 或 `Done:`，不得只写"参考某工作流"。
12. 写入 CR 后运行：
    - `python <fons4ai-sdd-tasks>/scripts/validate_sdd_artifacts.py --change-file <CR-file>`
13. 停止在变更规划阶段，提示用户确认执行。

## DDL 与 SQL 规则

当变更影响持久化数据结构时，必须显式规划 DDL 闭环：

- 命名受影响的 `.specify/sql/<database_or_service>/<business_model>.sql` 当前结构快照文件。
- 更新技术设计说明书的 `结构变更详设`，写清现状结构证据、目标结构定义、字段/属性明细、索引与约束、迁移/回填/清理、兼容性和回滚影响。
- 新增表或新增数据服务结构时，必须给出目标结构设计表；存量结构变更时，必须区分当前结构证据和目标结构，不得从语言结构体/类、ORM/Mapper/Repository 元数据或字段定义反推当前 DDL。
- 若涉及存量表结构变更且已有可靠基线 DDL，命名执行型 DDL 草案输出路径。
- 增量任务必须包含：
  - 生成执行型 DDL 草案。
  - 确认 DDL 执行状态。
  - 同步 SQL 当前结构快照。
- agent 不直接执行生产 DDL；DDL 由用户/DBA 手动执行，或由项目既有迁移流程执行。
- DDL 未确认执行前，依赖该结构的实现任务不得标记为发布就绪。
- 不得从语言结构体/类、ORM/Mapper/Repository 元数据或字段定义推断 `CREATE TABLE`。
- 多个数据库 MCP 或候选数据库无法唯一确认时，先询问用户选择。
- SQL 文件和执行型 DDL 草案不得包含 MCP/Tool 标识、查询文本、来源路径或 provenance 头信息。

## 长期知识影响

CR 只记录长期知识影响，不创建知识同步任务或知识汇总交接任务。

可记录：

- 是否产生长期知识影响。
- 影响类型：业务规则、技术方案、数据结构、接口契约、治理规则或其他。
- 影响说明。

知识沉淀由 `fons4ai-knowledge-summary` 在用户显式触发后处理。

## 输出规则

- S0 只输出变更内容、实际文件、验证命令或检查方式、真实结果、未验证项和升级风险；不生成 CR、任务规划、快速变更记录或实施报告。
- 如果被澄清门禁阻塞，只输出阻塞原因、一个澄清问题、推荐选项和回答后会生成的产物。
- 正式 CR 标题和固定文本以中文为主。文件名、ID、路径和机器字段 `AC:`、`Files:`、`Verification:`、`Quality:`、`Done:` 保持不变。
- 可执行增量任务必须追加到 `<功能中文名>-任务规划.md`；不得只放在 CR 中。
- 草案 CR 不得包含可执行增量任务。
- S1/S2 不得编写业务代码。
- 未经确认，不得删除既有 AC、任务或文档。
- 结束时输出 CR 路径、变更类型、SDD 等级、已更新产物、新增任务 ID、长期知识影响、实现确认状态 `pending`，并包含固定提示：`确认执行后默认执行全部未完成任务；如需指定范围，请回复：执行 T001,T002。`
